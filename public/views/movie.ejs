<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Movie</title>
  <link rel="stylesheet" href="../css/movie.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
  <header> 
    <a href="/">Home</a>
    <% if (moviedata.userName != "" && moviedata.roles == "Admin") { %>
    <a href="/create">Add Movie</a>
    <a href="/movieupdate">Update Movie</a>
    <a href="/moviedelete">Delete Movie</a>
    <% }%>
    <% if (moviedata.roles == "User" ){ %>
    <a href="/favorite/<%=moviedata.user_id%>">Favorite</a>
    <% }%>
    <% if (!moviedata.userName) { %>
    <a href="/register">Sign Up</a>
    <a href="/login">Sign In</a>
    <% } %>
    <% if (moviedata.userName) { %>
    <ul>
      <li>
      <img src="../images/noimage.jpg" alt="" class="profile">
      <ul class="dropdown">
        <li><a href="/user/<%=moviedata.user_id%>">Edit User</a></li>
        <li><a href="/deleteuser/<%=moviedata.user_id%>"onclick="return confirm('You delete really')">Delete User</a></li>
        <li><a href="/logout">Sign out</a></li>
      </ul>
      </li>
    </ul>
    <% } %>
  </header>
  <div class="mainbox">
    <div class="box1">
      <div>
        <% if (movie.imageFile) { %>
          <img src="/images/<%= movie.imageFile %>">
        <% } else {%>
          <img src="/images/noimage.jpg">
        <% } %>
      </div>
      <div>
        
       </div>
    </div>
    <div class="box2">
      <% if (favoriteStatus == "Unfavorite this movie!") { %>
        <h2 class="fade-in-right "><i class="fa-solid fa-xmark"></i>⠀<%=favoriteStatus%></h2>
      <% } %>
      <% if (favoriteStatus == "Add to your favorite!") { %>
        <h2 class="fade-in-right "><i class="fa-regular fa-heart"></i>⠀<%=favoriteStatus%></h2>
      <% } %>
      
      <h1><%=movie.title%></h1>
        <p>Director: <%=movie.director%></p>
        <form method="post" action="/favorite">
          <input hidden type="text" name="movie_id" value="<%=movie.movie_id %>">
          <input hidden type="text" name="user_id" value="<%=moviedata.user_id %>">
          <% if (favoriteStatus == "") { %>
            <button type="submit" class="btn">Favorite</button>
          <% } %>
          <% if (favoriteStatus == "Unfavorite this movie!") { %>
            <button type="submit" class="btn">Favorite</button>
          <% } else if(favoriteStatus == "Add to your favorite!"){%>
            <button type="submit" class="btn2">Unfavorite</button>
          <% } %>
        </form>
    </div>
  </div>
</body>
</html>
